[package]
name = "rox-anchor-core"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
description = "Shared IDs, states, labels, and errors for ROX Anchor."

[lib]
name = "rox_anchor_core"
path = "src/lib.rs"

[features]
default = []

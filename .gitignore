# =============================================================================
# rox-anchor .gitignore
# Reserved / future Solana Anchor program repo for RustyOnions / ROX
# =============================================================================

# =============================================================================
# OS junk
# =============================================================================
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# =============================================================================
# Editor / IDE
# =============================================================================
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json
!.vscode/tasks.example.json
!.vscode/launch.example.json

.idea/
*.swp
*.swo
*~
.history/
.project
.classpath

# =============================================================================
# Logs
# =============================================================================
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
bun-debug.log*
crash.log
debug.log

# =============================================================================
# Environment / secrets / credentials
# =============================================================================
.env
.env.*
!.env.example

secrets/
private/
credentials/
tokens/
auth/

*.secret
*.token
*.pem
*.key
*.p12
*.pfx
*.crt
*.csr

# =============================================================================

# =============================================================================
# Hardened secret / key material patterns — NEVER COMMIT
# =============================================================================

# dotenv / local shell config variants
.envrc
.direnv/
*.env
*.env.local
*.env.*.local
.env.local
.env.development
.env.production
.env.test
.envrc.local

# API keys, provider tokens, credentials, and auth material
api-keys/
apikeys/
keys/
keypairs/
wallets/
mnemonics/
seeds/
seed/
recovery/
keystore/
key-store/
key_store/

*.apikey
*.api-key
*.credentials
*.creds
*.passwd
*.password
*.mnemonic
*.seed
*.seed.json
*.recovery
*.recovery-phrase
*.priv
*.private
*.private.json
*.auth
*.session
*.cookie
*.cookies

# CLI/tool credential stores accidentally copied into the repo
.netrc
.aws/
.gcp/
.azure/
.kube/
.docker/
.cargo/credentials
.cargo/credentials.toml

# Package-manager auth files can contain registry tokens
.npmrc
.pnpmrc
.yarnrc

# RPC/provider/local deployment secrets
rpc-url.txt
rpc-url.local
provider-url.txt
provider-url.local
alchemy*.txt
quicknode*.txt
helius*.txt
triton*.txt
ankr*.txt
infura*.txt

# Solana / Anchor key material — NEVER COMMIT
# =============================================================================

# Common Solana keypair names
id.json
keypair.json
wallet.json
payer.json
authority.json
mint-authority.json
upgrade-authority.json
program-authority.json
deploy-authority.json
admin.json
owner.json
validator-keypair.json
faucet-keypair.json

# Generic keypair patterns
*.keypair
*.keypair.json
*-keypair.json
*.wallet.json
*.authority.json

# Anchor/Solana generated deploy keypairs
target/deploy/*.json
target/deploy/*-keypair.json
programs/**/target/deploy/*.json
programs/**/target/deploy/*-keypair.json


# Additional local Solana / Anchor keypair naming patterns
*-wallet.json
*-payer.json
*-authority.json
*-mint-authority.json
*-upgrade-authority.json
*-program-authority.json
*-program-keypair.json
*-validator-keypair.json
*-faucet-keypair.json
local-wallet*.json
local-payer*.json
local-authority*.json
local-keypair*.json
dev-wallet*.json
dev-payer*.json
dev-authority*.json
dev-keypair*.json

# Local Solana config accidentally copied into repo
.config/solana/
solana/
.solana/

# =============================================================================
# Rust / Cargo build output
# =============================================================================
target/
debug/
release/

**/target/
**/debug/
**/release/

*.rlib
*.rmeta
*.profraw
*.profdata

# Keep Cargo manifests and lockfiles tracked when runtime is authorized.
# Do NOT ignore:
# Cargo.toml
# Cargo.lock

# =============================================================================
# Anchor build/test output
# =============================================================================
.anchor/
.anchor-cache/
.anchor-test/
test-ledger/
ledger/
local-ledger/
validator-ledger/

target/idl/
target/types/
target/verifiable/
target/sbf-solana-solana/
target/sbf-solana-solana/release/
target/deploy/

programs/**/target/
programs/**/Cargo.lock.bak

# =============================================================================
# Node / TypeScript / package manager artifacts
# =============================================================================
node_modules/
.pnpm-store/
.npm/
.yarn-cache/
.bun/

.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

dist/
build/
out/
coverage/
.nyc_output/
.vite/
.turbo/
.cache/
.parcel-cache/
.rollup.cache/

*.tsbuildinfo

# Keep package manager lockfiles tracked when JS/TS tooling is authorized.
# Do NOT ignore:
# package-lock.json
# pnpm-lock.yaml
# yarn.lock
# bun.lockb

# =============================================================================
# Test output
# =============================================================================
test-results/
test-results-*/
playwright-report/
junit.xml
.vitest/

# =============================================================================
# Generated artifacts / bundles
# =============================================================================
artifacts/
releases/
packed/
*.tgz
*.tar.gz
*.zip

# =============================================================================
# Temporary files
# =============================================================================
tmp/
temp/
.tmp/
*.tmp
*.temp
*.bak
*.backup
*.orig
*.rej
*.old

# =============================================================================
# Local databases / runtime state
# =============================================================================
*.sqlite
*.sqlite3
*.db
*.db-shm
*.db-wal
*.sled/
*.redb
*.rocksdb/

local-state/
runtime-state/
session-state/

# =============================================================================
# Keep placeholders
# =============================================================================
!.gitkeep
!.keep

# ROX Anchor generated local review bundles.
CODEBUNDLE*.md

# =============================================================================
# ROX Anchor testnet deployment drill local artifacts
# =============================================================================
# Phase 7 drills may create local-only deployment notes, testnet artifacts,
# generated Anchor outputs, or operator-side key references. Keep all of them
# outside version control unless an artifact is intentionally promoted later
# as a redacted, non-secret audit document.
.rox-anchor-testnet/
.anchor-testnet/
deploy-drills/
testnet-deploy/
testnet-artifacts/
testnet-ledger/
test-validator-ledger/

*.deploy-plan.json
*.deploy-manifest.json
*.deploy-output.json
*.deploy-receipt.json
*.testnet-keypair.json
*.testnet-wallet.json
*.testnet-authority.json
*.testnet-payer.json

# =============================================================================
# ROX Anchor private pilot local artifacts — NEVER COMMIT
# =============================================================================
# BUILD_PLAN3 private pilot work may use local-only operator artifacts.
# Keep all pilot keys, RPC URLs, deployment outputs, receipts, ledgers, and
# unredacted audit captures outside tracked source.

.rox-anchor-pilot/
.rox-anchor-private-pilot/
private-pilot/

pilot-artifacts/
pilot-rpc/
pilot-keys/
pilot-keypairs/
pilot-wallets/
pilot-secrets/
pilot-receipts/
pilot-audit/
pilot-deploy/
pilot-ledger/
pilot-tmp/

*.pilot-config.local.toml
*.pilot-config.local.json
*.pilot-rpc.txt
*.pilot-provider.txt
*.pilot-keypair.json
*.pilot-wallet.json
*.pilot-authority.json
*.pilot-payer.json
*.pilot-receipt.json
*.pilot-audit.json
*.pilot-deploy-output.json
*.pilot-ledger.json

# =============================================================================
# ROX Anchor BUILD_PLAN4 actual private testnet local artifacts — NEVER COMMIT
# =============================================================================
# BUILD_PLAN4 may create real private devnet/testnet operator files outside
# tracked source. These names are intentionally ignored in case an operator
# creates a local workspace inside the repo by mistake.

private-testnet.toml
private-testnet.json
actual-private-testnet.toml
actual-private-testnet.json
*.private-testnet.local.toml
*.private-testnet.local.json
*.actual-private-testnet.local.toml
*.actual-private-testnet.local.json
*.pilot-authority-notes.local.txt
authority-notes.local.txt
